<%= form_for(workshop) do |f| %>
  <% if workshop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(workshop.errors.count, "error") %> prohibited this workshop from being saved:</h2>

      <ul>
      <% workshop.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :offering_id %><br>
    <%= f.select :offering_id, Offering.all.collect { |o| [ o.id, o.id ] } %>
  </div>

  <div class="field">
    <%= f.label :lab_id %><br>
    <%= f.select :lab_id, Lab.all.collect { |l| [ l.id, l.id ] } %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
